#include "main.h"

/**
 * _abs -  Computes the absolute value of an integer.
 * @i: target integer.
 * Return: Absolute value of integer, 0 otherwise.
 */

int _abs(int i)
{
	if (i > 0)
		return (i);
	else if (i < 0)
		return (-i);
return (0);
}
